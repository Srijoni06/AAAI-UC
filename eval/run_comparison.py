"""Four-condition comparison (NOT in milestone 1).

Planned: run the same seeded suite under
  1. last_write_wins
  2. majority_vote
  3. static_confidence  (fixed weights, no learning)
  4. reliability-aware   (our contribution)
and log resolution accuracy (vs gold labels) plus downstream literature-QA
accuracy for each.
"""

# TODO(milestone 4): implement the comparison run + logging.
